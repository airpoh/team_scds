# Core data processing
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
protobuf>=3.19.0

# Machine learning and NLP
transformers>=4.30.0
# keep torch/vision in lockstep to avoid mismatch
torch==2.8.*
torchvision==0.23.*
scikit-learn>=1.3.0
sentence-transformers>=2.2.0

# Specialized NLP libraries
emoji>=2.6.0
langdetect>=1.0.9
deep-translator>=1.11.4   # << replaced googletrans/libretranslate

# Change point detection (optional; install via conda to avoid build)
# ruptures>=1.1.8  # install with: conda install -c conda-forge ruptures
# or try: changepoints>=0.1.7

# Efficient string matching (optional but recommended)
pyahocorasick>=2.0.0

# Web framework and visualization
streamlit>=1.25.0
plotly>=5.15.0

# (Removed fasttext; code disables it by default)
# fasttext>=0.9.2

# Image processing (for emoji/image analysis)
Pillow>=9.5.0
# consider headless in servers:
# opencv-python-headless>=4.8.0
opencv-python>=4.8.0

# HTTP
requests>=2.31.0  # << added (used in visual_emoji_analysis)

# Progress bars and utilities
tqdm>=4.65.0

# Development and testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: for production deployment
gunicorn>=20.1.0
uvicorn>=0.23.0
fastapi>=0.100.0
